<h1>Simulation der Solarwash-Gerätesteuerung</h1>
<div id="output">
<table>	
  <tr><td class="middle">
      <table>
        <%OURDEVICES.each do |name, data|%>
        <tr>
          <% if name == session[:device]%>
            <td><b><%= name %></b></td>
        <% else%>
          <td><%= name %></td>
        <%end%>
        </tr>
      <% end %>
    </table>
  </td></tr>
</table>	
</div>	
<div id="buttons">
  <%= link_to "", 'push_button/up', :class => "button", :id => "up"%>
  <%= link_to "", 'push_button/down', :class => "button", :id => "down"%>
  <%= link_to "", 'push_button/left', :class => "button", :id => "left"%>
  <%= link_to "", 'push_button/right', :class => "button", :id => "right"%>
  <%= link_to "", 'push_button/center', :class => "button", :id => "center"%>
</div>	
